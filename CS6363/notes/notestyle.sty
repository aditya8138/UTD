\ProvidesPackage{notestyle}

%This package doesn’t do anything as long as your syntax is right.
%Load the package in the first lines of your preamble (even before
%the \documentclass command). It then checks for obsolete LaTeX
%packages and outdated commands.
\RequirePackage[l2tabu, orthodox]{nag}

\RequirePackage{tabu}

\RequirePackage[letterpaper,margin=1in]{geometry}

\RequirePackage{fancyhdr}

\RequirePackage{makeidx}

\RequirePackage{extramarks}

%
% AMS packages.
%
\RequirePackage{amsmath}
\RequirePackage{amsthm}
\RequirePackage{amsfonts}

\RequirePackage{tikz}

%This package introduces the \includegraphics command, which is needed
%for inserting figures.
\RequirePackage{graphicx}

\RequirePackage{algorithm}
\RequirePackage{algpseudocode}

\RequirePackage{listings}

%
%Font Settings.
%
%\RequirePackage{zi4} %Use 'Inconsolata' as typewriter font.
\RequirePackage[scaled=0.8]{DejaVuSansMono} % Use DejaVu Sans Mono as typewriter font.

\RequirePackage[american]{babel}
\RequirePackage[useregional,% use regional style instead of default ISO format
            showzoneminutes=false,% don't show time zone minutes
            showdow% show the day of week name if supported
           ]{datetime2}

%The microtype package improves the spacing between words and letters.
%It does a lot more and most people won’t notice the difference.
%But still, the resulting document will be easier to read and looks
%better when microtype is loaded. Load this package after fonts,
%if any, as the package behavior is dependent on this font.
\RequirePackage{microtype}

\RequirePackage{enumitem}

\RequirePackage{textcomp}

\RequirePackage{booktabs}


%As a rule of thumb it should be loaded at the end of the preamble,
%after all the other packages. A few exceptions exist, such as the
%cleveref package that is also mentioned in this post. Hence, cleveref
%should be loaded after hyperref.
\RequirePackage{hyperref}

%This package introduces the \cref command. When using this command
%to make cross-references, instead of \ref or \eqref, a word is placed
%in front of the reference according to the type of reference: fig.
%for figures, eq. for equations
\RequirePackage{cleveref}

\usetikzlibrary{automata,positioning}

%
% Time Format Settings
%
% Use standard mappings for the time zones (if supported)
\DTMlangsetup*{mapzone,zone=standard}
\selectlanguage{american}
\DTMsetstyle{en-US}

%Setup hyperref package, and colours for links
\definecolor{linkcolour}{rgb}{0,0.2,0.5}
\hypersetup{
%    bookmarks=true,         % show bookmarks bar?
    unicode=false,          % non-Latin characters in Acrobat’s bookmarks
    pdftoolbar=true,        % show Acrobat’s toolbar?
    pdfmenubar=true,        % show Acrobat’s menu?
    pdffitwindow=false,     % window fit to page when opened
    pdfstartview={FitH},    % fits the width of the page to the window
    %pdftitle={Homework},    % title
    pdfauthor={Hanlin He},     % author
    %pdfsubject={Subject},   % subject of the document
    pdfcreator={Hanlin He},   % creator of the document
    %pdfproducer={Producer}, % producer of the document
    %pdfkeywords={Algorithm, Homework}, % list of keywords
    pdfnewwindow=true,      % links in new PDF window
    colorlinks=false,       % false: boxed links; true: colored links
    linkcolor=red,          % color of internal links (change box color with linkbordercolor)
    linkbordercolor={1 0 0},
    citecolor=green,        % color of links to bibliography
    filecolor=magenta,      % color of file links
    urlcolor=cyan,           % color of external links
    plainpages=false,
    pageanchor=false,       %put an anchor on every page
}

%
% Basic Document Settings
%

% Use these for theorems, lemmas, proofs, etc.
\theoremstyle{plain} % default
\newtheorem{theorem}{Theorem}[subsection]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{claim}[theorem]{Claim}
\newtheorem{corollary}[theorem]{Corollary}

\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}


\newcommand{\question}{\vspace{0.1in}\noindent \textbf{Question:}\ }
\newcommand{\analysis}{\vspace{0.1in}\noindent \textbf{Analysis:}\ }

\newcommand{\ProblemDescription}{\vspace{0.1in}\textbf{Problem Description:}\ }

% Alias for the Solution section header
\newcommand{\solution}{\vspace{0.1in}\textbf{\large Solution}}

% **** IF YOU WANT TO DEFINE ADDITIONAL MACROS FOR YOURSELF, PUT THEM HERE:

\newcommand\E{\mathbb{E}}

% Useful for algorithms
\newcommand{\alg}[1]{{\bfseries \footnotesize #1}}

\renewenvironment{proof}{{\vspace{0.1in}\noindent \textbf{Proof:}\qquad}}{\qedsymbol}

\newcommand{\BaseCase}{\vspace{0.1in}\noindent \underline{Base Case:}\qquad  }
\newcommand{\InductionStep}{\vspace{0.1in}\noindent \underline{Induction step:}\qquad }
\newcommand{\InductionConclusion}{\vspace{0.1in}\noindent \underline{Conclusion:}\qquad }

\newcommand{\Claim}{\vspace{0.1in}\noindent \textbf{Claim:}\qquad}

% Renew the commit font to texttt.
\algrenewcommand{\algorithmiccomment}[1]{\hfill\(\triangleright\) \texttt{\footnotesize{#1}}\normalsize{ \(\triangleleft\) }}

\newcommand*{\ProcedureName}[2]{\textsc{#1} (#2)}
